<main class="scan"><a href="/">← Back to Job Hunter</a><p class="eyebrow">Assistant setup</p><h1>Make finding roles a routine.</h1><p>Connect your assistant to the HTTP MCP endpoint below. It can inspect the live schema, add verified roles, update existing listings, index forms, and record each scan.</p><div class="copy"><code id="endpoint"><%= @endpoint %></code><button data-copy="#endpoint">Copy</button></div><h2>Suggested prompt</h2><pre id="prompt">Connect to <%= @endpoint %> using my AGENT_TOKEN. Ask me for my location, role, seniority, stack, mission, salary, and remote preferences. Then create a daily inbox check and a weekly verified job scan. Always call get_job_fields first, preserve my notes and history, verify direct application links, and summarize ambiguous matches instead of guessing.</pre><button data-copy="#prompt">Copy prompt</button></main>
